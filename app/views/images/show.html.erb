<h1>Valid Image</h1>
<%= image_tag @image.image_url %>
<% @image.tag_list.each do |tag| %>
  <%= link_to(tag, images_path(tag_list: tag),class: "js-tag")%>
<% end %>
<%= link_to "Delete Image", image_path(@image.id), class: "js-del", method: :delete, data: {confirm: 'Are you sure?'}%>
